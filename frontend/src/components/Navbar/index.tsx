import './styles.css';

const Navbar = () => {
    return (
        <nav className="navbar bg-primary">
            <div>
                <a href="link" className="nav-log-text">
                    <h4>SONDA</h4>
                </a>
            </div>
        </nav>
    );
};

export default Navbar;